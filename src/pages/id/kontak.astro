---
import Layout from '../../layouts/StaticPage.astro';
import ContactForm from '../../components/ContactForm.astro';
import { useTranslations } from '../../i18n/utils';

const t = useTranslations('id');
---

<Layout title={t('contact.title')} description={t('contact.description')} locale="id">
	<p>{t('contact.intro')}</p>
	<ContactForm locale="id" />
</Layout>
